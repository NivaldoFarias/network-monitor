{
  "name": "network-monitor",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --outfile dist/index.js --minify",
    "dev": "bun run --watch index.ts",
    "setup-systemd": "bun run src/setup-systemd-service.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}